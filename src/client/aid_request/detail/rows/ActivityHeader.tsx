import * as React from 'react';
import Row from '../components/Row';
import RowTitle from '../components/RowTitle';

export default function ActivityHeader(): JSX.Element {
  return (
    <Row>
      <RowTitle>Activity</RowTitle>
    </Row>
  );
}
